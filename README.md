# lu96-emacs-config

My personal Emacs configuration.

## Install

Make sure to backup your existing config first:

    mv ~/.emacs.d ~/.emacs.d.backup

Then clone:

    git clone https://github.com/lu96-wow/lu96-emacs-config.git ~/.emacs.d

Start Emacs and wait for packages to install on first run.